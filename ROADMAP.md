# DeepSeek Chat 研究路线图 / Research Roadmap

> 2026-06-08 | 聊天交互质量的算法实验场

---

## 路线图结构说明

这不是"加功能"的路线图，而是**研究假设的验证路径**。每个 track 都以可证伪的假设开头，以可量化的结论结束。

已完成的基础设施（Phase 1-6 全部）作为实验平台存在，不在此列出。以下所有 track 是并行推进的。

---

## Track 1：Context 最优组合（高效）

**核心假设**：Context 组装方式（顺序/格式/密度）对 DeepSeek V4 的输出质量有显著影响，存在非直觉的最优解。

### 实验矩阵

| 变量 | 取值 | 当前默认 | 控制方式 |
|------|------|---------|---------|
| System Prompt 位置 | 消息列表最前 / 消息列表最后 | 最前 | `messages.py` build_messages() |
| 记忆注入方式 | 独立 system msg / 拼在 user msg 末尾 | 独立 | `context/manager.py` |
| 工具定义策略 | 每次发 / 缓存复用 / 按需注入 | 每次发 | Tool Router |
| 历史压缩 | 摘要 / 截断 / 关键句 / 丢弃 | 截断 | `context/manager.py` compress_history() |
| 工具结果保留 | 全保留 / 摘要 / 丢弃 | 全保留 | Tool Loop |

### 实验计划

```
Week 1-2: 建立基线
  └─ 固定配置跑 50 组测试对话 → 人工评分 → 基准确认

Week 3-4: 单变量对照
  └─ A/B 测试：每次只变一个变量 → 量化影响

Week 5-6: 多变量交叉
  └─ 高影响变量组合测试 → 寻找交互效应

Week 7-8: 最优配置确定
  └─ 输出推荐配置 → 固化为默认配置
```

### 成功标准

- 找到 2-3 个有统计显著性的 context 优化策略
- 每条策略附带 token 节省量和质量影响数据
- 最终推荐配置的回复质量不低于基线，token 消耗降低 20%+

---

## Track 2：中断与恢复（稳定）

**核心假设**：模拟真实对话中的中断/修正场景，DeepSeek V4 在保留部分推理后重新开始比完全重新开始质量更高。

### 实验场景

| 场景 | 触发条件 | 预期行为 | 测量指标 |
|------|---------|---------|---------|
| 用户修正 | 用户说"等等不对" | AI 收回错误推理，基于修正重新生成 | 修正后回复的准确率 |
| 消息追加 | 用户发完消息后立即再发一条 | 合并 input 再推理 vs 独立处理 | 合并 vs 不合并的质量差异 |
| 网络断连 | SSE 中断 | 从断点续传 vs 重发 | 延迟差 + 输出完整性 |
| 用户打断 | AI 生成中用户发新消息 | 保留已推理内容重新应答 vs 丢弃 | 上下文连贯性 |
| 长对话漂移 | 30轮+对话 | system prompt 约束是否仍有效 | 约束遵守率 |

### 实验计划

```
Phase A: 场景实现
  └─ SSE 断点标记协议 / 消息队列 / 中断处理器

Phase B: 对照实验
  └─ 每个场景设置对照组（当前行为）和实验组（新行为）

Phase C: 用户体验验证
  └─ 真实用户场景测试 → 收集主观满意度
```

### 成功标准

- 中断场景下恢复后的回复连贯性不低于正常回复
- 用户修正后 AI 能正确收回错误部分并基于修正回答（非简单拼接）
- 30 轮对话后 system prompt 约束遵守率 > 90%

---

## Track 3：资源效率（快速）

**核心假设**：大多数用户对话不需要 V4 Pro 的完整推理能力，通过模型路由可以在不明显降质的前提下显著降低 token 消耗和延迟。

### 实验矩阵

| 优化策略 | 实现方式 | 预期收益 | 风险 |
|---------|---------|---------|------|
| 模型路由 | 简单问题 → Flash | 延迟 -50%，token 成本 -70% | 复杂问题误判 → 回复质量下降 |
| 工具跳过 | AI 先判断要不要工具 | 无需工具时跳过一次 API 调用 | 漏掉真正需要的工具调用 |
| Prompt 缓存 | system prompt + tools 缓存 | 首 Token 延迟 -60% | 缓存命中率依赖场景 |
| 历史压缩 | 长对话智能丢弃低价值消息 | token 消耗 -30% | 丢掉关键上下文 |
| 并发工具 | 独立工具并行执行 | 工具阶段延迟 = 最慢工具的时间 | 不适用于有依赖关系的工具链 |

### 实验计划

```
Phase 1: 模型路由
  └─ 训练简单分类器（问题复杂度） → 路由到 Flash/Pro
  └─ 回退机制：Flash 失败 → 自动升级到 Pro

Phase 2: 工具调用决策
  └─ 让 AI 在每次回复前先输出 {needs_tool: true/false}
  └─ 对比：跳过工具 → 少了什么

Phase 3: Prompt 缓存
  └─ 测量 DeepSeek API 的缓存命中率
  └─ 首 token 延迟改善

Phase 4: 综合
  └─ 最优策略组合 → 端到端指标
```

### 成功标准

- 在不降低回复质量的前提下，复杂任务平均 token 消耗降低 30%+
- Flash 路由准确率 > 90%（复杂问题不应被路由到 Flash）
- 用户感知延迟降低 40%+

---

## Track 4：推理可视化与体验

**核心假设**：实时展示 DeepSeek V4 的 reasoning_content 能显著降低用户等待焦虑，且不干扰对话流畅感。

### 实验方向

| 方向 | 具体做法 | 测量指标 |
|------|---------|---------|
| 实时推理 | reasoning_content 流式渲染 | 用户感知等待时间（主观评分） |
| 折叠策略 | >200 字推理默认折叠 | 展开率、阅读完成率 |
| 分段展示 | 长推理按主题分段 | 用户是否更容易找到关键信息 |
| 中途引导 | 推理中插入用户反馈 | 回复改善率 |
| 上下文标注 | 高亮 AI 使用的上下文来源 | 用户信任度 |

### 实验计划

```
已有: reasoning 流式渲染 + 独立卡片
待验证:
  └─ A/B 测试：开启/关闭推理可视化的用户满意度差异
  └─ 折叠阈值研究：多少字的推理用户愿意阅读
  └─ 大上下文导航原型
```

### 成功标准

- 开启推理可视化后用户等待满意度提升 20%+
- 推理折叠率合理（不全部关闭也不全部展开）
- 大上下文导航功能让用户清楚知道"AI 用了什么信息"

---

## Track 5：记忆引用与需求挖掘

**核心假设**：让 AI 在回复中显式引用记忆（"基于你之前提到的..."）能显著提升用户对"AI 了解我"的感知，且不显得刻意。

### 实验方向

| 方向 | 具体做法 | 测量指标 |
|------|---------|---------|
| 记忆溯源 | 回复中标注记忆来源 | 用户信任度变化 |
| 需求挖掘 | 根据记忆追问更深层问题 | 对话深度、用户继续对话的概率 |
| 矛盾检测 | 发现用户陈述与记忆矛盾时提问 | 用户满意 AI 的"细心" |
| 遗忘曲线 | 不同时间跨度的记忆衰减策略 | 记忆召回准确率变化 |
| 记忆可见 | 用户可实时查看 AI 用了哪些记忆 | 用户对记忆系统的满意度 |

### 实验计划

```
Phase 1: 记忆溯源
  └─ 在回复中标注记忆来源（已有 Honcho 基础）
  └─ A/B 测试：标注 vs 不标注

Phase 2: 积极引用
  └─ 不只是匹配关键词，而是主动引用关联记忆
  └─ 测量引用准确率和用户反馈

Phase 3: 需求挖掘
  └─ 当用户表达模糊情绪时，根据记忆追问具体原因
  └─ 测量追问后的对话深度变化
```

### 成功标准

- 记忆引用准确率 > 85%
- 用户感知"AI 了解我"的主观评分提升 30%+
- 需求挖掘追问不被用户感知为"啰嗦"

---

## Track 6：拟人性格

**核心假设**：通过 system prompt 约束 + 记忆驱动的人格参数调整，可以实现跨会话一致的 AI 性格，且不谄媚的人格长期用户满意度更高。

### 实验矩阵

| 变量 | 测量方法 | 当前基线 |
|------|---------|---------|
| 人格漂移 | 100 轮后同一 prompt 的回复 embedding 余弦距离 | 未知 |
| 谄媚指数 | 用户说错误观点时 AI 同意的比例 | 估计 >50% |
| 持久性 | 30 轮对话后初始 system prompt 约束的遵守率 | 未知 |
| 用户满意度 | 不同人格 + 不同用户群的满意度评分 | 无 |

### 实验计划

```
Phase 1: 建立基线
  └─ 人格漂移距离基线
  └─ 谄媚指数基线

Phase 2: 反谄媚机制
  └─ 实现 agreeableness 参数
  └─ A/B 测试：agreeableness=high vs low

Phase 3: 记忆驱动人格
  └─ 用户偏好记忆 → 调整人格参数
  └─ 测量人格演变是否符合用户期望
```

### 成功标准

- 100 轮对话后人格 embedding 漂移 < 0.3
- 谄媚指数从基线降低 40%+
- 反谄媚人格的用户长期满意度不低于谄媚人格

---

## Track 7：幻觉控制

**核心假设**：多种降幻觉策略的组合效果优于单一策略，且有工具支持的对话幻觉率显著低于纯推理对话。

### 实验矩阵

| 策略 | 预期降幻觉幅度 | 成本 | 副作用 |
|------|---------------|------|--------|
| 工具调用 | -40% | 额外 API 调用 | 延迟增加 |
| 强制引用 | -30% | 更多 token | 回复变长 |
| 推理链 | -20% | 更多 reasoning token | 等待时间增加 |
| 交叉验证 | -50% | 2x API 调用 | 成本翻倍 |
| 拒绝策略 | 100%* | 用户受挫 | 拒答太多 → 用户流失 |

### 实验计划

```
Phase 1: 幻觉检测
  └─ 构建测试集（事实查询 + 代码生成 + 创意写作）
  └─ 人工标注真实/幻觉/不确定

Phase 2: 单策略对照
  └─ 每种策略单独测试 → 量化效果

Phase 3: 策略组合
  └─ 找到 1+1>2 的组合

Phase 4: 领域差异
  └─ 代码 vs 事实 vs 观点 → 不同策略的最优组合
```

### 成功标准

- 事实类查询幻觉率从基线降低 50%+
- 自我矛盾率（同 session 内前后不一致）< 5%
- 最优策略组合的幻觉率 < 10%（事实类）

---

## 实验基础设施（已有，持续维护）

以下能力是实验平台而非研究目标本身。保持稳定运行即可。

| 能力 | 用途 | 状态 |
|------|------|------|
| Tool Router | 工具调用对照实验 | ✅ 运行中 |
| Planner | 多步推理实验 | ✅ 运行中 |
| MCP Gateway | 外部工具对接 | ✅ 运行中 |
| Sandbox | 代码执行实验 | ✅ 运行中 |
| Honcho Memory | 记忆注入实验（Track 5 的基础） | ✅ 运行中 |

---

## 实验结论的应用

实验验证通过的结论以默认配置形式沉淀到产品中。实验阶段的特性不进稳定版。

---

## 里程碑

```
2026 Q3: 完成 Track 1 + Track 4
  └─ Context 最优配置确定
  └─ 推理可视化 A/B 测试结果

2026 Q4: 完成 Track 2 + Track 3
  └─ 中断恢复协议稳定
  └─ 模型路由上线（Flash/Pro 自动切换）

2027 Q1: 完成 Track 5 + Track 6
  └─ 记忆引用准确率 > 85%
  └─ 人格漂移 < 0.3

2027 Q2: 完成 Track 7
  └─ 事实幻觉率 < 10%
  └─ 全策略组合推荐配置
```
